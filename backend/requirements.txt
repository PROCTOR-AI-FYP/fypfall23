# Runtime dependencies (installed in the production image). Pinned so every
# Railway build is reproducible; bump deliberately and re-run the test suite.
fastapi==0.141.1
uvicorn[standard]==0.53.0
asyncpg==0.31.0
redis==8.1.0
# [crypto] pulls in `cryptography`, needed to verify Supabase's ES256-signed access tokens.
PyJWT[crypto]==2.14.0
cryptography==50.0.1
pydantic==2.13.5
pydantic-settings==2.15.0
python-dotenv==1.2.3
Jinja2==3.1.6
python-multipart==0.0.32
email-validator==2.3.0
python-socketio==5.17.0
aiomqtt==2.5.1
anthropic==1.8.0
httpx==0.28.1
