[pytest]
asyncio_mode = auto
# The app keeps module-level asyncpg/Redis clients (as it does in production),
# so every test and fixture must share one event loop; per-test loops leave
# those clients bound to a closed loop ("Event loop is closed").
asyncio_default_fixture_loop_scope = session
asyncio_default_test_loop_scope = session
